#!/bin/sh
if [ -n "$_RTK_WRAP" ]; then
  PATH="${PATH#/opt/rtk:}"; export PATH; exec diff "$@"
fi
if [ $# -eq 0 ]; then
  PATH="${PATH#/opt/rtk:}"; export PATH; exec diff
fi
export _RTK_WRAP=1; exec rtk diff "$@"
