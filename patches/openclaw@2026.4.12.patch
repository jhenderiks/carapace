--- a/dist/channel.runtime-BRzZhqmb.js
+++ b/dist/channel.runtime-BRzZhqmb.js
@@ -531,11 +531,17 @@
 				let healthCheckEnabled = getBotUpdateAt != null;
 				let healthCheckInFlight = false;
 				let healthCheckTimer;
+				let pingTimer;
+				let lastPongAt = Date.now();
 				let initialUpdateAt;
 				const clearTimers = () => {
 					if (healthCheckTimer !== void 0) {
 						clearTimeout(healthCheckTimer);
 						healthCheckTimer = void 0;
+					}
+					if (pingTimer !== void 0) {
+						clearInterval(pingTimer);
+						pingTimer = void 0;
 					}
 				};
 				const stopHealthChecks = () => {
@@ -587,6 +593,23 @@
 				};
 				ws.on("open", () => {
 					opened = true;
+					lastPongAt = Date.now();
+					ws.on("pong", () => {
+						lastPongAt = Date.now();
+					});
+					if (typeof ws.ping === "function") {
+						pingTimer = setInterval(() => {
+							if (Date.now() - lastPongAt > 6e4) {
+								opts.runtime.error?.("mattermost websocket pong timeout — terminating");
+								ws.terminate();
+								return;
+							}
+							try {
+								ws.ping();
+							} catch {
+							}
+						}, 3e4);
+					}
 					captureWsEvent({
 						url: opts.wsUrl,
 						direction: "local",
--- a/dist/channel-plugin-runtime-dBklJDgB.js
+++ b/dist/channel-plugin-runtime-dBklJDgB.js
@@ -741,6 +741,7 @@
 			}),
 			resolveReplyToMode: (account, chatType) => resolveMattermostReplyToMode(account, chatType === "direct" || chatType === "group" || chatType === "channel" ? chatType : "channel")
 		},
+		allowExplicitReplyTagsWhenOff: false,
 		resolveReplyTransport: ({ threadId, replyToId }) => ({
 			replyToId: replyToId ?? (threadId != null ? String(threadId) : void 0),
 			threadId
